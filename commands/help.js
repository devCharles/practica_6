function help() {
  console.log(`
    Usage: koders [command]

    Commands:
      ls      List all koders
      add     Add a new koder
      rm      Remove a koder
      reset   Remove all koders
  `);
}

module.exports = help;
